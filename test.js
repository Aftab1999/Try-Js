// Start an interval that prints every 1 second
const intervalId = setInterval(() => {
  console.log("This will print forever, but safely every 1 second...");
}, 1000);

// To stop the loop, call clearInterval like this:
// clearInterval(intervalId);
